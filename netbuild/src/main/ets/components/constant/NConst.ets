import BaseOptions from "../base/BaseOptions";
import { HashMap } from "@kit.ArkTS";

export class NConst {
  static baseUrl: string
  static headMap: HashMap<string, string>
  static opt: BaseOptions
}